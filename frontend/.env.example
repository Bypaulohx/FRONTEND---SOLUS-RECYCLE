# ===========================================
# API do backend (feita por outro grupo)
# ===========================================
#
# Desenvolvimento (frontend na porta 5173, backend em outra URL):
#   O Vite faz proxy de /api para o backend. Defina onde o backend está rodando.
#   Ex.: se o backend roda no IntelliJ em http://localhost:8080
#
# VITE_API_URL=/api
# VITE_BACKEND_URL=http://localhost:8080
#
# Produção (build estático):
#   Use a URL completa da API do backend (onde o outro grupo hospedar).
#
# VITE_API_URL=https://url-do-backend.herokuapp.com
# VITE_API_URL=https://api.seudominio.com
#

VITE_API_URL=/api
VITE_BACKEND_URL=http://localhost:8080
